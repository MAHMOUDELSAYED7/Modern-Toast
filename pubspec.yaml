name: modern_toast
description: A beautiful, modern, and customizable toast notification package for Flutter with smooth animations and multiple toast types.
version: 0.0.5
homepage: https://github.com/MAHMOUDELSAYED7/Modern-Toast
repository: https://github.com/MAHMOUDELSAYED7/Modern-Toast
issue_tracker: https://github.com/MAHMOUDELSAYED7/Modern-Toast/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animate_do: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  # This package is a plugin and does not contain any platform-specific code.
  # To ensure that your plugin is correctly recognized by Flutter, you must
  # include this section.
